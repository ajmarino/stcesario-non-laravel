module.exports = {
    theme: {
        extend: {
            colors: {
                'black70': 'rgba(0, 0, 0, .7)',
            }
        }
    },
    variants: {
        // Some useful comment
    },
    plugins: [
        // Some useful comment
    ]
}
